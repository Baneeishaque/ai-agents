name: git_reword
description: Dart FFI bindings for git-reword-commit-cli - A cross-platform library for non-interactive git commit rewording.
version: 1.0.0
homepage: https://github.com/Baneeishaque/AI-Agents

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
